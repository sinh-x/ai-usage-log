# Use devenv for automatic environment setup
use flake

# Watch for changes in these files and reload
watch_file devenv.nix
watch_file flake.nix
watch_file flake.lock

# Load .env file if it exists
dotenv_if_exists
